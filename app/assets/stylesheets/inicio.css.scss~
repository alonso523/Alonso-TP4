// Place all the styles related to the sessions controller here.
// They will automatically be included in application.css.
// You can use Sass (SCSS) here: http://sass-lang.com/
h1 {
	padding-left:5%;
 	color: white;
}

body {
  background-color: #C5DEF5;
  color: white;
  font-family: verdana, arial, helvetica, sans-serif;
  font-size: 16px;
  line-height: 18px;
}
//titulo del inicio de pagina
div#titulo{
			background-color:#4682B4;
			color:white;
			font: {
				weight:bold;
			}
			padding: {
				top:3px;
				bottom:3px;
				left:5px;
				right:5px;
			}
		}

//Estructura del cuerpo de la pagina de inicio, aqui van las imagenes
div#imagenes{
			color:white;
			font: {
				weight:bold;
			}
			padding: {
				top:50px;
				bottom:50px;
				left:15px;
				right:15px;
			}
}
